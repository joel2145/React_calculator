import React from "react";
import { TextInput } from "../components/UIkit";

const SignUp = () => {
    return (
        <TextInput
            fullWidth={ } label={ } multiline={ } requred={ }
            rows={} value={} type={} onChange={}
        />
    )
}

export default SignUp;